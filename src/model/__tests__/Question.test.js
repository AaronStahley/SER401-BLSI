import RootStore from "../../store/root/RootStore";

describe('test question', () => {
    
    let root;

    /*
    beforeAll(() => {
        root = new RootStore('../../assets/db/database.db')
    })

    
    test("retrieval", () => {
        let question;
        root.questionStore.getOrFindAll()
            .then(res => {
                question = res;
            })
        expect(question).toBeDefined();
    })*/

    test("placholder", () => {
        let val = true;
        expect(val).toBeTruthy()
    })
})
